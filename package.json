{
  "name": "csc-diagnostics",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "vite build && node scripts/stamp-csp-hash.mjs",
    "preview": "vite preview --port 3000",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "typescript": "^5.5.4",
    "vite": "^7.3.3",
    "vitest": "^4.1.6"
  }
}
